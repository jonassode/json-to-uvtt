# json-to-uvtt

Simple light webpage to upload and render `.svg` map files.

## Run locally

```bash
python3 -m http.server 8080
```

Then open `http://localhost:8080` in your browser and upload an SVG file.
